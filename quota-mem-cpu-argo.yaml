apiVersion: v1
kind: ResourceQuota
metadata:
  name: mem-cpu-argo
  annotations:
    workflows.argoproj.io/description: |
       This resource quota prevents using limited cpu and ram in a namespace.
spec:
  hard:
    requests.cpu: "1000m"
    requests.memory: "8Gi"
    limits.cpu: "2000m"
    limits.memory: "10Gi"
    count/workflows.argoproj.io:"10000"
