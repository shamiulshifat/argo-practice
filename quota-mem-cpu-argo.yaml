apiVersion: v1
kind: ResourceQuota
metadata:
  name: mem-cpu-argo
  namespace: argo
spec:
  hard:
    requests.cpu: "2000m"
    requests.memory: "16Gi"
    limits.cpu: "3000m"
    limits.memory: "20Gi"
